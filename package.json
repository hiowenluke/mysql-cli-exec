{
  "name": "mysql-cli-exec",
  "version": "0.1.6",
  "description": "A MySQL command-line tool for Node.js. Execute the MySQL sql statements in CLI, return the result as an object array.",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "net": "^1.0.2",
    "shelljs": "^0.8.3"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0"
  }
}
